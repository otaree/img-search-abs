{
  "name": "img-search-abs",
  "version": "1.0.0",
  "description": "A image search abstraction layer",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test && mocha --exit server/**/*.test.js",
    "test-watch": "nodemon --exec 'npm test'"
  },
  "author": "otaree",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "google-images": "^2.1.0",
    "mongoose": "^5.0.6"
  },
  "devDependencies": {
    "expect": "^22.4.0",
    "mocha": "^5.0.1",
    "supertest": "^3.0.0"
  }
}
